% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workbook_harvest.R
\name{profile_rate_workbook}
\alias{profile_rate_workbook}
\title{Profile an Excel workbook for candidate rating tables}
\usage{
profile_rate_workbook(
  file,
  sheets = NULL,
  min_cells = 3L,
  min_rows = 1L,
  min_cols = 2L,
  include_threshold = 0.5,
  title_lookback = 3L
)
}
\arguments{
\item{file}{Path to an \code{.xlsx}, \code{.xlsm}, or \code{.xlsb} workbook.}

\item{sheets}{Optional character vector of sheet names to scan. By default all
sheets are scanned except sheets beginning with an underscore.}

\item{min_cells}{Minimum number of nonblank cells required for a candidate.}

\item{min_rows}{Minimum number of occupied rows required for a candidate.}

\item{min_cols}{Minimum number of occupied columns required for a candidate.}

\item{include_threshold}{Confidence threshold used to populate the editable
\code{include} column. Candidates below the threshold remain in the profile.}

\item{title_lookback}{Number of rows above a candidate searched for a nearby
descriptive title.}
}
\value{
A data frame with class \code{raterevision_workbook_profile}. Each row is a
candidate table. The \code{include}, \code{table_name}, \code{extract_range}, and
\code{header_row} fields may be edited before calling \code{\link[=extract_rate_tables]{extract_rate_tables()}}.
}
\description{
Scans an arbitrary \code{.xlsx} workbook and identifies rectangular regions that
look like rating tables. The profiler is deliberately heuristic: it uses
connected nonblank regions, header structure, numeric density, and nearby
labels to make educated guesses. The returned profile is intended to
be reviewed and, when necessary, edited before extraction.
}
