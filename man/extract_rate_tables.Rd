% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workbook_harvest.R
\name{extract_rate_tables}
\alias{extract_rate_tables}
\title{Extract candidate rate tables from a profiled workbook}
\usage{
extract_rate_tables(
  profile,
  file = attr(profile, "file"),
  only_included = TRUE,
  min_confidence = NULL
)
}
\arguments{
\item{profile}{A \code{raterevision_workbook_profile} returned by
\code{\link[=profile_rate_workbook]{profile_rate_workbook()}}. The profile may be edited before extraction.}

\item{file}{Optional workbook path. By default the source path stored on the
profile is used.}

\item{only_included}{Logical; when \code{TRUE}, extract only rows where \code{include}
is \code{TRUE}.}

\item{min_confidence}{Optional additional confidence threshold. Set to \code{NULL}
to rely only on the editable \code{include} column.}
}
\value{
A named list of rectangular data frames with class
\code{raterevision_tables}. A manifest is stored in the \code{manifest} attribute.
}
\description{
Reads the candidate regions selected in a \code{\link[=profile_rate_workbook]{profile_rate_workbook()}} result and
returns a named list of rectangular data frames. The object intentionally uses
the same list-oriented interface as \code{\link[=rate_tables_wide]{rate_tables_wide()}}, while retaining
source-sheet/range metadata so that a migration can be reviewed and refined.
}
\details{
This function does not claim that the extracted rectangles are already valid
normalized \code{ratingtables} factor tables. It is an ingestion step: uncertain
candidates should be reviewed, renamed, resized, or excluded in the profile
before extraction.
}
