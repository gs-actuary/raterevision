% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.R
\name{solve_rate_target}
\alias{solve_rate_target}
\title{Solve a target aggregate rate change}
\usage{
solve_rate_target(
  current_premium,
  target,
  rate_function,
  interval = c(0.5, 1.5),
  premium_extractor = NULL,
  tol = 1e-08,
  maxiter = 100L,
  extend_interval = c("no", "yes", "upX", "downX"),
  parameter_name = "multiplier"
)
}
\arguments{
\item{current_premium}{Current aggregate premium, a positive finite scalar.}

\item{target}{Desired aggregate rate change as a decimal, e.g. \code{0.06}.}

\item{rate_function}{Function of one numeric parameter. It may return a
premium scalar/vector directly, or any object handled by
\code{premium_extractor}.}

\item{interval}{Initial two-value search interval for the parameter.}

\item{premium_extractor}{Optional function that converts the result of
\code{rate_function} to a single aggregate premium.}

\item{tol}{Root-finding tolerance.}

\item{maxiter}{Maximum root-finding iterations.}

\item{extend_interval}{Passed to \code{stats::uniroot(extendInt = ...)}; one of
\code{"no"}, \code{"yes"}, \code{"upX"}, or \code{"downX"}.}

\item{parameter_name}{Label used in printed output.}
}
\value{
A \code{raterevision_target_solution} object.
}
\description{
Finds a single numeric parameter that makes an arbitrary proposed rating
workflow achieve a desired aggregate rate change from a supplied current
premium. The solver does not know or care whether the proposed workflow uses
the same specification, a structurally different specification, driver
averaging, custom R code, or territory-specific base rates. The user defines
the one-dimensional adjustment inside \code{rate_function}.
}
\details{
A common territory-base use is to let \code{x} multiply every territory-specific
base rate, preserving the proposed territorial shape while solving one
overall rate-level parameter. If several base rates are allowed to move
independently against one aggregate target, the problem is underdetermined
and should not be passed to this scalar solver.
Independent targets (for example one target per coverage) can be solved by
calling this function once per independently adjustable parameter.
}
