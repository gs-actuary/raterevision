% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/migration_diagnostics.R
\name{diagnose_rating_output}
\alias{diagnose_rating_output}
\title{Diagnose differences between a reference rater and a candidate rater}
\usage{
diagnose_rating_output(
  comparison,
  predictors = NULL,
  absolute_tolerance = 0.01,
  relative_tolerance = 1e-06,
  top_n = 5L,
  model_sample = 50000L,
  max_model_levels = 40L,
  interaction_top = 4L,
  seed = 1L
)
}
\arguments{
\item{comparison}{Output from \code{\link[=compare_rating_outputs]{compare_rating_outputs()}}.}

\item{predictors}{Optional character vector of columns to investigate. By
default all non-ID, non-premium columns are used, including \code{coverage}.}

\item{absolute_tolerance}{Dollar tolerance below which a premium difference is
treated as a match.}

\item{relative_tolerance}{Relative tolerance applied to the absolute reference
premium. A row is wrong only when its absolute error exceeds both tolerances.}

\item{top_n}{Number of variables retained in the high-level suspect table.}

\item{model_sample}{Maximum number of rows used by the background models.}

\item{max_model_levels}{Maximum factor levels retained in a background model;
rarer levels are collapsed to \verb{<OTHER>}. Numeric variables with many unique
values are quantile-binned for diagnostic modeling.}

\item{interaction_top}{Number of leading variables considered for pairwise
interaction diagnostics. Set to \code{0} to skip interaction models.}

\item{seed}{Random seed used only when down-sampling for background models.}
}
\value{
An object of class \code{raterevision_rater_diagnosis} containing summary
counts, variable/level diagnostics, model signals, interaction signals, and
the fitted lightweight models for advanced inspection.
}
\description{
Uses grouped discrepancy scans and lightweight statistical models to identify
rating variables associated with missing or incorrect candidate premiums. The
models are diagnostic tools, not production predictive models: no tuning grid
or cross-validation is performed, and the default print method reports only
high-level findings.
}
\details{
The input is normally created by \code{\link[=compare_rating_outputs]{compare_rating_outputs()}}, with all useful
rating variables carried through via its \code{keep_cols} argument. In a migration
workflow, \code{current_premium} should represent the trusted/reference premium and
\code{proposed_premium} the candidate \code{ratingtables} premium.
}
