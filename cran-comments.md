## R CMD check results

This is an initial development draft. Before CRAN submission, run `devtools::check()` locally and on a clean CI matrix, then replace this note with the actual check results.

## Notes

`ratingtables` is used only in examples/vignettes and is listed in Suggests. Excel I/O is provided by the single imported dependency `openxlsx2`.
